
@{
    ViewBag.Title = "CreateAccount";
}

@if (TempData["error"] != null)
{
    <div class="alert alert-danger alert-dismissible">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
        <h4><i class="icon fa fa-ban"></i> Alert!</h4>
        @TempData["error"]
    </div>
}
@using (Html.BeginForm())
{
    @*@Html.AntiForgeryToken()*@

    <div class="form-actions no-color text-center">
        <input type="submit" value="Continue" class="btn btn-primary text-center" />

    </div>
}
<h2 class="text-center" style="color:;">
    For Admin Only <br />Connection string:
</h2>
<h4 class="text-center" style="word-break:break-all;color:;">
  @*@{
      var aa = System.Configuration.ConfigurationManager.ConnectionStrings["DefaultConnection"].ToString();
      string i = aa.ToString();
  }*@  
    @System.Configuration.ConfigurationManager.ConnectionStrings["DefaultConnection"]
</h4>